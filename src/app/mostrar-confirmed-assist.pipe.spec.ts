import { MostrarConfirmedAssistPipe } from './mostrar-confirmed-assist.pipe';

describe('MostrarConfirmedAssistPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarConfirmedAssistPipe();
    expect(pipe).toBeTruthy();
  });
});
