import { MostraUserTypePipe } from './mostra-user-type.pipe';

describe('MostraUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MostraUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
