import { MostrarIsActivePipe } from './mostrar-is-active.pipe';

describe('MostrarIsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarIsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
